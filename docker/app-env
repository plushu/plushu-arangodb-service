#!/usr/bin/env bash
set -eo pipefail; [[ -n "$PLUSHU_TRACE" ]] && set -x

cat <<"EOF"
export ARANGODB_URL=http://$ADDON_ARANGODB_LINK_PORT_8529_TCP_ADDR:$ADDON_ARANGODB_LINK_PORT_8529_TCP_PORT
export ARANGODB_HOST=$ADDON_ARANGODB_LINK_PORT_8529_TCP_ADDR
export ARANGODB_PORT=$ADDON_ARANGODB_LINK_PORT_8529_TCP_PORT
EOF
